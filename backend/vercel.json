# Create correct config
echo '{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "builds": [
    {
      "src": "*.html",
      "use": "@vercel/static"
    }
  ]
}' > vercel.json